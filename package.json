{
  "name": "mern-stack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "babel": {
    "presets": [
      "react",
      [
        "env",
        {
          "targets": {
            "browsers": [
              "last 2 versions"
            ]
          }
        }
      ]
    ]
  },
  "scripts": {
    "compile": "./node_modules/.bin/babel src --presets env --out-dir static",
    "watch": "./node_modules/.bin/babel --watch src --presets env --out-dir static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oscar Lora",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-watch": "^2.0.7"
  }
}
